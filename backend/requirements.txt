# torch arrives as a sentence-transformers dependency. PyPI's default wheel bundles
# ~6 GB of NVIDIA CUDA libraries, which are useless on this AMD machine. Pin the CPU
# build explicitly so a fresh install doesn't pull them back in.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu

# ── Web framework ────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0
python-dotenv==1.0.0
python-multipart==0.0.9

# ── Authentication ───────────────────────────────────────
# Local verification of Supabase access tokens (auth.py). cryptography is
# needed for asymmetric-signed projects.
PyJWT==2.12.1
cryptography>=42.0.0

# ── Data access ──────────────────────────────────────────
# Runtime talks to Supabase over PostgREST via httpx (see database.py).
# psycopg2 is only for the bulk-ingestion scripts, which need a direct connection.
httpx==0.27.0
supabase>=2.0.0
psycopg2-binary

# ── LLM / agents ─────────────────────────────────────────
groq==0.37.1
langchain-groq==0.1.6
langgraph==0.2.0
sentence-transformers==2.7.0

# ── Document processing ──────────────────────────────────
# pdf2image and pytesseract drive the scanned-PDF OCR fallback in
# services/pdf_service.py. Both need system binaries: poppler-utils + tesseract-ocr.
pdfplumber==0.11.0
pdf2image==1.17.0
pytesseract==0.3.10
Pillow==10.3.0

# ── Testing ──────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
